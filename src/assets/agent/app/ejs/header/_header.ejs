<span>
  <ul id="tabroot" class="nav nav-tabs nav-bar">
    {{#each tabs}}
      <li id="firsttab" class="active">
        <a href='#' {{action 'open' this}}>{{name}}</a>
      </li>
    {{/each}}
  </ul>
</span>
